import Contact from "@/components/Contact/Contact";

import { Metadata } from "next";
import React from "react";
export const metadata: Metadata = {
  title: "Gate of Africa - Contact",
};
export default function ContactPage() {
  return <Contact />;
}
